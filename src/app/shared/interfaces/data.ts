export interface Employee {
  firstName: string;
  lastName: string;
}

export interface Inventory {
  plu: number;
  supplier: string;
  name: string;
  inStock: number;
  price: number;
  currency: string;
}
